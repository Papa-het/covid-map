import styled from 'styled-components';

export const MapContainer = styled.div`
  .leaflet-container {
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    right: 0;
    z-index: 1;
  }
`;
