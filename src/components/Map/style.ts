import styled from 'styled-components';

export const MapContainer = styled.div`
  .leaflet-container {
    position: absolute;
    left: calc(20vw);
    top: 50px;
    bottom: 0;
    right: 0;
    z-index: 1;
  }
`;
