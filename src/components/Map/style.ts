import styled from 'styled-components';

export const MapContainer = styled.div`
  .leaflet-container {
    position: absolute;
    left: calc(20vw);
    top: 60px;
    bottom: 0;
    right: calc(20vw);
    z-index: 1;
  }
`;
